import state from './state'
import * as actions from './actions'
import * as getters from './getters'
import * as mutations from './mutations'

const myCustumModule = {
    namespaced: true,
    action,
    getters,
    mutations,
    stat
}

export default myCustumModule